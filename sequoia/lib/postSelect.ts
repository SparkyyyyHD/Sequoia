/** Columns used whenever we load posts for lists, search, and detail. */
export const POST_LIST_FIELDS =
  "id, content, author_name, created_at, helpful_count, not_helpful_count, category, subcategory, title, custom_tags" as const;
